fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic-settings==2.9.1
# Pydantic의 EmailStr 타입이 이 패키지를 필요로 한다.
email-validator==2.2.0

# Database
sqlalchemy==2.0.41
psycopg2-binary==2.9.10
alembic==1.16.2

# Auth
# passlib은 2020년 이후 유지보수가 멈춰 bcrypt 5.x와 호환되지 않으므로 bcrypt를 직접 사용한다.
# python-jose 역시 유지보수가 멈추고 알려진 취약점이 있어 PyJWT를 사용한다.
bcrypt==5.0.0
pyjwt==2.10.1

# Test
pytest==8.3.5
httpx==0.28.1

[pytest]
# 프로젝트 루트를 import 경로에 추가한다.
# 이게 없으면 tests/에서 `from app.main import app`이 실패해서
# 매번 PYTHONPATH=. 를 붙여 실행해야 한다.
pythonpath = .

testpaths = tests

# 테스트가 .env.test 를 읽게 만드는 APP_ENV 설정은 tests/conftest.py 맨 위에서 한다.
# (여기서 지정하려면 pytest-env 플러그인이 추가로 필요해서 의존성을 늘리지 않았다.)
